# Softer defaults when registry.npmjs.org is slow or flaky (common cause of partial / failed installs)
registry=https://registry.npmmirror.com/
fetch-timeout=600000
fetch-retries=10
network-concurrency=2
